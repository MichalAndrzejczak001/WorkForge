apiVersion: apps/v1
kind: Deployment
metadata:
  name: offer-preparation-mysql
  labels:
    app: offer-preparation-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: offer-preparation-mysql
  template:
    metadata:
      labels:
        app: offer-preparation-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_DATABASE
              value: offer-preparation_db
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: offer-preparation-mysql-secret
                  key: mysql-user
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: offer-preparation-mysql-secret
                  key: mysql-password
            - name: MYSQL_ROOT_PASSWORD
              value: rootpassword
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          emptyDir: {}   # dla prostoty; w prawdziwym środowisku użyj PersistentVolume
